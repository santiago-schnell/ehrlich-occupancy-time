# Python >= 3.10 required
#
# Install all dependencies with:
#   pip install -r requirements.txt
#
# Versions below were used to produce the published figures.
# Newer minor versions are generally compatible.

numpy>=2.0          # np.trapezoid introduced in NumPy 2.0
scipy>=1.12         # RectBivariateSpline; solve_ivp RK45 / Radau
matplotlib>=3.8
seaborn>=0.13
